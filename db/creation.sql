CREATE TABLE activity (id INTEGER PRIMARY KEY AUTOINCREMENT, timestamp TIMESTAMP NOT NULL, prompt VARCHAR NOT NULL, mode VARCHAR NOT NULL, model VARCHAR NOT NUll, temperature REAL, top_k INTEGER, output VARCHAR NOT NULL, error VARCHAR);