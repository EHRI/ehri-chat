CREATE TABLE activity (id INTEGER PRIMARY KEY AUTOINCREMENT, timestamp TIMESTAMP NOT NULL, prompt VARCHAR NOT NULL, mode VARCHAR NOT NULL, model VARCHAR NOT NUll, output VARCHAR NOT NULL, error VARCHAR);